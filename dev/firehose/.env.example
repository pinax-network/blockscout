# Copy to `.env` (gitignored) or export these directly. These are the only Firehose-specific
# settings. The connector uses TLS, listens on 127.0.0.1:8082, and runs eight workers.
FIREHOSE_ENDPOINT=<chain>.firehose.<provider>:443

# Long-lived API key sent as x-api-key metadata.
FIREHOSE_API_KEY=
